myApp.controller('TableController', function(){
  /* These represent the boxes on the build page
  * Ideally, these should change to true when clicked and then the data
  *   could be sent to the back end for storage for guides.
  */
  this.levels = [false, false, false, false, false, false, false, false, false, false];
});
